# DevContainer Environment Configuration
# Use this configuration when working inside VS Code DevContainer

# Environment identification
ENV_TYPE=devcontainer

# Docker image configuration
IMAGE_NAME=session-api:latest
IMAGE_REGISTRY=localhost:5000

# LocalStack endpoint (in-cluster DNS, works from DevContainer)
LOCALSTACK_ENDPOINT=http://localstack.localstack-system.svc.cluster.local:4566

# Kubernetes configuration
KUBECONFIG_PATH=./kubeconfig-internal
VELA_NAMESPACE=default

# Testing configuration
# "cluster" mode uses in-cluster Kubernetes DNS
TEST_ENDPOINT_MODE=cluster

# Port forwarding (not needed in DevContainer)
# Port-forwards are handled automatically by kubectl inside the container
