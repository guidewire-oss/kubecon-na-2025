# CI/CD Environment Configuration
# Use this configuration in GitHub Actions, GitLab CI, or other CI systems

# Environment identification
ENV_TYPE=ci

# Docker image configuration
# Override these in CI/CD system environment variables
IMAGE_NAME=session-api:latest
# In CI, typically use registry path: gcr.io/project/session-api:latest
IMAGE_REGISTRY=gcr.io/example

# LocalStack endpoint (in-cluster DNS, works from CI pods)
LOCALSTACK_ENDPOINT=http://localstack.localstack-system.svc.cluster.local:4566

# Kubernetes configuration
# In CI, kubeconfig is usually auto-mounted by the orchestrator
KUBECONFIG_PATH=${KUBECONFIG_PATH:-.}
VELA_NAMESPACE=default

# Testing configuration
# "cluster" mode uses in-cluster Kubernetes DNS
TEST_ENDPOINT_MODE=cluster

# Optional: Service account for authentication
# Usually auto-configured by CI platform
SERVICE_ACCOUNT_PATH=/var/run/secrets/kubernetes.io/serviceaccount

# Optional: Image pull secrets for private registries
# Configure in CI platform environment
IMAGE_PULL_SECRETS=""
