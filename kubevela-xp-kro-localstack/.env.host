# Host Machine Environment Configuration
# Use this configuration when working on your local machine (not in DevContainer)
# Supports: WSL, macOS, Linux with k3d installed

# Environment identification
ENV_TYPE=host

# Docker image configuration
IMAGE_NAME=session-api:latest
# k3d registry port (default 5000, may vary if registry is configured differently)
# Override if needed: IMAGE_REGISTRY=localhost:32500
IMAGE_REGISTRY=localhost:5000

# LocalStack endpoint
# Host machine needs port-forward or localhost endpoint
# Default: use kubectl port-forward to access
LOCALSTACK_ENDPOINT=http://localhost:4566

# Kubernetes configuration
KUBECONFIG_PATH=./kubeconfig-host
VELA_NAMESPACE=default

# Testing configuration
# "portforward" mode uses localhost endpoints (requires port-forwards)
TEST_ENDPOINT_MODE=portforward

# Port forwarding configuration
# These are used by port-forward-helpers.sh to set up tunnels
PORT_FORWARD_LOCALSTACK=4566:4566
PORT_FORWARD_SESSION_API_KRO=9080:8080
PORT_FORWARD_SESSION_API_XP=9081:8080

# Optional: If running tests, set this to auto-setup port-forwards
AUTO_SETUP_PORTFORWARD=true
